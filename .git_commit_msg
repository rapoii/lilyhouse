v1.0.51: UI consistency audit — unified design tokens across all modals

Changes:
- Replace ALL hardcoded Color(0xFFF2F2F7) → AppColors.background across 10 files
  (add_costume_sheet, calendar_screen, installment_list_screen,
   installment_detail_screen, main_scaffold, draggable_sheet_container,
   costume_card)
- Replace inline TextStyle(fontSize:17,w600) → AppTypography.navTitle
  in add_costume_sheet nav bar
- Replace inline TextStyle(fontSize:15,w600,pink) → AppTypography.actionButton
  in add_costume_sheet Simpan button
- Add AppTypography import to add_costume_sheet
- Add AppColors import to draggable_sheet_container
- Fix add_costume_sheet size picker row: add title "Ukuran" with
  value moved to additionalInfo (matches pattern of other modals)
- Fix installment_list_screen date picker row: add title "Jatuh tempo"
  with formatted date moved to additionalInfo
- installment_list_screen: CupertinoFormSection → CupertinoListSection
  (consistent with other modals)

Result: All 3 form modals (Kostum Baru, Booking Manual, Cicilan Baru)
now share identical widget patterns, icon sizes, text alignment,
color tokens, and typography styles.

Verified: flutter analyze 0 issues, 89/89 tests passing.
Pixel-verified text alignment x=211-213 across all rows in all modals.
