v1.0.55 build 73: smooth fade-in transition for deferred CupertinoDatePicker

- sheet_picker.dart: added FadeTransition with 220ms easeOut curve to
  _DeferredDatePickerSheet so when the picker swaps in after the modal
  entrance completes, it fades in softly instead of popping/teleporting in.
