v1.0.55 build 64: title bar — perfect horizontal centering

The title "Ukuran" was 28px to the left of the screen's true horizontal
center in build 63. Root cause: the Row's `spaceBetween` + `Expanded` +
`Center` c...[truncated]