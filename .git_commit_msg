v1.0.55 build 69: Booking Manual picker lag — RepaintBoundary per section

Wrap each CupertinoListSection in manual_booking_modal in its own
RepaintBoundary so opening the date picker no longer forces the whole
form (5 textfields + 2 image slots + 4 picker rows) to repaint behind
the modal barrier.
